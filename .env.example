# Vercel / `vercel dev` — never commit real keys
# Create at https://console.anthropic.com/ → API keys
ANTHROPIC_API_KEY=

# Optional — default is a fast, cost-effective model
# ANTHROPIC_MODEL=claude-3-5-haiku-20241022
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# Optional: if the static app is hosted elsewhere, set the Vercel deployment origin
# VITE_CHAT_API_URL=https://your-deployment.vercel.app
