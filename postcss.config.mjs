/**
 * Vite portfolio uses plain CSS (`src/index.css`). Do not load Tailwind here
 * unless you add `@tailwindcss/postcss` to devDependencies.
 */
const config = {
  plugins: {},
};

export default config;
